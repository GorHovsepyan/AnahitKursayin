#pragma once

#include "afxwin.h"
#include "resource.h"
/*

class CEnterIDDialog : public CDialogEx {
public:
    CEnterIDDialog() : CDialogEx(IDD_DIALOG_ENTER_ID) {}

    CString m_gameID; // Holds the entered game ID

protected:
    virtual void DoDataExchange(CDataExchange* pDX) override {
        CDialogEx::DoDataExchange(pDX);
        DDX_Text(pDX, IDC_EDIT_GAME_ID, m_gameID); // Bind edit control to m_gameID
    }

    DECLARE_MESSAGE_MAP()
};
*/